enum AppClient { muziris, kalyan }

enum ThemeModeType { light, dark }

enum UserDeviceType { phone, tablet, desktop }

enum PlatformType { android, ios }

enum Logging { debug, info, warning, error }

enum Language { english, malayalam }

enum HttpMethodType { get, post, delete, postList }
